# Heavy optional packages. The launcher installs these best-effort so one
# missing wheel does not prevent the app from starting. Not bundled in the
# frozen desktop build (too large).
rembg[cpu]
openai-whisper
marker-pdf
